from array import array
arr1 = array('i' , map(int , input  ("Enter element of first array:  ")split()))
arr2 = array('i' , map(int , input ("Enter element of  second array;  ")split()))
common = array('i' )
for i is arr1 ;
    if   i  is  arr2  and  i  not  is
                  common . append(i )
        print("common elements : " , list (common))
